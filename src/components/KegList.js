import React from 'react';
import Keg from './Keg'

function KegList() {
  return (
    <React.Fragment>
      <h3>KegList</h3>
      {/* <Keg 
      name: {props.name}
      brand: {props.brand}
      price: {props.price}
      alcohol content: {props.alcoholContent}
      pints: {props.pints} /> */}
    </React.Fragment>
  );
}

export default KegList;
